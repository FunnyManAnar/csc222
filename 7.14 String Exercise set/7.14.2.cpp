#include <iostream>
#include <string>
using namespace std;

void Backwordinator() {
    string str1;
    str1 = "baller";

}

int main()
{
    while (index < state.length()) {
    if (state[index] == 'i') {
        count++;
    }
    index++;
}
}